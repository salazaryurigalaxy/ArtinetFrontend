# Copy this file to .env.local and set the real backend URL when it's available.
# Until then, every service in src/services falls back to mock data.
NEXT_PUBLIC_API_URL=https://api.artinet.example.com
